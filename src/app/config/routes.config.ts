/** Ruta de módulo de autenticación. */
export const AUTH: string = 'auth';

/** Ruta de login. */
export const LOGIN: string = 'login';

/** Ruta del módulo persona. */
export const PERSONAS: string = 'personas';

/** Ruta del listado de personas. */
export const PERSONAS_LIST: string = 'list';

/** Ruta del formulario de creación de personas. */
export const PERSONAS_FORM: string = 'form';

/** Ruta del módulo pais. */
export const PAISES: string = 'paises';

/** Ruta del listado de países. */
export const PAISES_LIST: string = 'list';
