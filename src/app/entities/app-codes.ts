/**
 * Constantes para códigos de error de Excepciones y StatusMessage (app).
 */
export enum AppCodes {

    /** Código de acción cancelada por el usuario (identifica excepciones). */
    UserCancel = 'user cancel',

    /** Runtime error  */
    ErrRunTimeException = 'ERR.RUNTIME.EXCEPTION',

    /** Token válido */
    TokenValido = 'token_valido'
}
